"""Tool registration modules for the Sage MCP FastMCP server.

Each ``register(mcp)`` function attaches a related family of tools/resources
to a FastMCP instance. Split by concern to keep each module readable.
"""
