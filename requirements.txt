# --- MCP core --------------------------------------------------------------
# Pinned to modern MCP SDK / FastMCP that support streamable-http, SSE and stdio
mcp>=1.10.0
fastmcp>=2.10.0

# --- Sage integrations -----------------------------------------------------
sage-data-client>=0.6.0

# --- Data processing -------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0

# --- Validation / config ---------------------------------------------------
pydantic>=2.5.0
pyyaml>=6.0.0

# --- HTTP clients ----------------------------------------------------------
requests>=2.31.0
httpx>=0.25.0

# --- ASGI stack (transitively required by fastmcp; listed explicitly)
starlette>=0.36.0
uvicorn>=0.29.0

# --- Test dependencies -----------------------------------------------------
pytest>=7.4.0
pytest-asyncio>=0.23.0
ndp-ep